{
  "name": "simple-math-exp-complie",
  "version": "1.0.0",
  "description": "complie a simple math expression(lexer parse and syntax parse)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "webpack server --mode=development --open",
    "sync:github": "git add -A && git commit && git push -u origin master"
  },
  "author": "wone",
  "license": "ISC",
  "devDependencies": {
    "html-webpack-plugin": "^5.3.2",
    "webpack": "^5.42.0",
    "webpack-cli": "^4.7.2",
    "webpack-dev-server": "^3.11.2"
  },
  "dependencies": {
    "ifuncs": "^1.0.3"
  }
}
